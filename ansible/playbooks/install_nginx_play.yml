---
- name: nginx_playbook
  hosts: all
  become: yes
  become_user: root
  tasks:
    - name: Install nginx
      yum:
        name: nginx
        state: present
    - name: Start nginx
      service:
        name: nginx
        state: started
        enabled: yes

